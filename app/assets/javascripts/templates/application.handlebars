<div class="navbar navbar-static-top navbar-inverse">
  <div class="navbar-inner">
    {{#linkTo 'index' class='brand'}}Octopus{{/linkTo}}
    <ul class="nav">
      {{#linkTo 'theories.index' tagName='li'}}
        {{#linkTo 'theories.index'}}Theories{{/linkTo}}
      {{/linkTo}}

      {{#linkTo 'experiments.index' tagName='li'}}
        {{#linkTo 'experiments.index'}}Experiments{{/linkTo}}
      {{/linkTo}}

      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          Media <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          {{#linkTo 'videos.index' tagName='li'}}
            {{#linkTo 'videos.index'}}Videos{{/linkTo}}
          {{/linkTo}}

          {{#linkTo 'slideshows.index' tagName='li'}}
            {{#linkTo 'slideshows.index'}}Slideshows{{/linkTo}}
          {{/linkTo}}
        </ul>
      </li>
    </ul>

    <div class="pull-right">
      {{render "shared/connector_widget" connector}}
    </div>

    <ul class="nav pull-right">
      {{#linkTo 'settings.index' tagName='li'}}
        {{#linkTo 'settings.index'}}Settings{{/linkTo}}
      {{/linkTo}}

    </ul>
  </div>
</div>

{{#if currentUser.isSignedIn}}
  <div class="alert alert-info">
    signed in <strong>{{currentUser.email}} / {{currentUser.authenticationToken}}</strong>
  </div>
{{/if}}

<div class="container">
  {{outlet}}
</div>