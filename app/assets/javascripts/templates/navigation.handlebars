{{#if canNavigate}}
  <ul class="nav">
    {{#linkTo 'participants.index' tagName='li'}}
      <a href="#">Participants</a>
    {{/linkTo}}

    {{#linkTo 'experiments.index' tagName='li'}}
      <a href="#">Experiments</a>
    {{/linkTo}}

    {{#linkTo 'recordings.index' tagName='li'}}
      <a href="#">Recordings</a>
    {{/linkTo}}

    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        Media <b class="caret"></b>
      </a>
      <ul class="dropdown-menu">
        {{#linkTo 'videos.index' tagName='li'}}
          <a href="#"><i class="icon-facetime-video"></i> Videos</a>
        {{/linkTo}}

        {{#linkTo 'slideshows.index' tagName='li'}}
          <a href="#"><i class="icon-picture"></i> Slideshows</a>
        {{/linkTo}}

        {{#linkTo 'audios.index' tagName='li'}}
          <a href="#"><i class="icon-music"></i> Audio</a>
        {{/linkTo}}
      </ul>
    </li>
  </ul>

  <div class="pull-right">
      {{#linkTo 'settings.index' class='btn btn-inverse'}}
        <i class="icon-cog icon-inverse"></i>
      {{/linkTo}}
      {{render "shared/connector_widget" connector}}
  </div>
{{/if}}
