<div class="row">
  <div class="col-lg-9">
    <ul class="nav nav-pills">
      {{#linkTo 'recordings.cloud' tagName='li'}}
        {{#linkTo 'recordings.cloud'}}
          <i class="icon-cloud"></i>&nbsp;Cloud
        {{/linkTo}}
      {{/linkTo}}

      {{#linkTo 'recordings.local' tagName='li'}}
        {{#linkTo 'recordings.local'}}
          <i class="icon-desktop"></i>&nbsp;Local
        {{/linkTo}}
      {{/linkTo}}
  </ul>

  </div>

  <div class="col-lg-3">
    <div class="pull-right">
      {{#if canRecord}}
        {{#linkTo newRecordingRoute class='btn btn-success'}}
          <i class="icon-plus"></i>&nbsp;New Recording
        {{/linkTo}}
      {{else}}
        <a class="btn btn-default" rel="tooltip" title="You cannot record, unless the collector is running.">
          <i class="icon-ban-circle"></i>&nbsp;New Recording
        </a>
      {{/if}}
    </div>
  </div>
</div>

<hr/>