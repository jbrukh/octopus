{{partial 'recordings/header'}}

{{#unless controller.length}}
  <div class="alert alert-info">
    There are no recordings.
  </div>
{{else}}
  <table class="table table-striped table-hover table-list">
    <thead>
      <tr>
        <th>Created At</th>
        <th>Owner</th>
        <th>Name</th>
        <th>Duration</th>
        <th>Participant</th>
        <th>Tags</th>
        <th>State</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each controller}}
        <tr>
          <td>
            <small>{{timeSince createdAt}}</small>
          </td>
          <td>{{owner}}</td>
          <td>{{name}}</td>
          <td>{{durationMs}}ms</td>
          <td>
            {{#if participant}}
              {{#linkTo 'participant' participant tagName='span' class='label label-info clickable'}}
                <i class="icon-user"></i> {{participant.fullName}}
              {{/linkTo}}
            {{/if}}
          </td>
          <td>
            {{#if taggings.length}}
              <span class="label label-info">
                <i class="icon-tag"></i>&nbsp;{{taggings.length}}
              </span>
            {{/if}}
          </td>
          <td>
            {{#if isUploading}}
              <div class="upload-progress progress progress-striped active">
                <div class="bar bar-success" style="width: 100%">
                  <i class="icon-upload"></i> uploading
                </div>
              </div>
            {{else}}
              <span class="label label-info">{{state}}</span>
            {{/if}}
          </td>
          <td>
            <div class="pull-right">
              {{#unless isUploading}}
                {{#linkTo 'recording.cloud' this class='btn btn-table btn-success'}}Explore{{/linkTo}}
              {{/unless}}
              <a href="#" class="btn btn-table btn-danger" {{action destroy this}}>
                <i class="icon-trash"></i>
              </a>
            </div>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{/unless}}