<div class="page-header">
  <div class="pull-right">
    {{#linkTo theories.new class="btn btn-success"}}
      <i class="icon-plus"></i> New Theory
    {{/linkTo}}
  </div>
  <h3>Theories</h3>
</div>

There are {{controller.length}} theories

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Created At</th>
    </tr>
  </thead>
  <tbody>
    {{#each controller}}
      <tr>
        <td>{{#linkTo 'theory' this}}{{name}}{{/linkTo}}</td>
        <td>{{timeSince created_at}}</td>
        <td>
          <div class="pull-right">
            {{#linkTo 'trial.start' this class="btn btn-mini btn-success"}}Start Trial{{/linkTo}}
            <button class="btn btn-mini btn-danger" {{action destroy this}}>Delete</button>
          </div>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>