# attr = Ember.attr

# App.Policy = Ember.Model.extend
#   id:         attr()
#   contents:   attr()
#   signature:  attr()

# App.Policy.url = "/api/policies"
# App.Policy.rootKey = 'policy'
# App.Policy.collectionKey = 'policies'
# App.Policy.camelizeKeys = true
# App.Policy.adapter = Ember.RESTAdapter.create()